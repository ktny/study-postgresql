services:
  db:
    container_name: db
    image: postgres:15
    restart: always
    tty: true
    volumes:
      - db-store:/var/lib/postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pgpassword

volumes:
  db-store:
